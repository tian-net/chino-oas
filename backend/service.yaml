apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: elchino
  labels:
    app: backend
spec:
  type: NodePort
  ports:
    - port: 30001
      targetPort: 8087
      nodePort: 30001
  selector:
    app: backend
